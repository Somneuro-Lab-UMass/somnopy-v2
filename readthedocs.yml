version: 2

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-22.04

python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt  # Optional: if you have extra requirements for docs
